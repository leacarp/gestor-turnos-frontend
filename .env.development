# Backend local (activo por defecto)
VITE_API_BASE_URL=http://localhost:3000/api

# Backend en Render (opción para cuando haga falta probar contra producción)
# Descomentá la línea de abajo y comentá la de arriba para usarla:
# VITE_API_BASE_URL=https://gestor-turnos-backend.onrender.com/api
